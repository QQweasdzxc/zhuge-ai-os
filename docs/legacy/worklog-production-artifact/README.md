# Legacy WorkLog Artifact Metadata

These manifest and checksum files are retained for traceability from the
previous `worklog-workspace` UAT package. They are historical records only and
are not loaded by the migrated WorkLog runtime or used as the current release
identity.
