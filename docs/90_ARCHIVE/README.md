# Archived Zhuge AI OS Material

Historical Reference Only.

The contents of this directory:

- must not be treated as Current Architecture;
- must not be treated as Implementation Authority;
- must not be treated as a Runtime Contract; and
- must not be used to restore an old workflow or route merely because it is documented here.

Current State always takes priority:

`docs/00_CURRENT/` + Current Source + Current Cloud Contract.
