# Shared i18n

`zh-TW.js` is the default Zhuge AI OS locale contract: `zh-TW`,
`Asia/Taipei`, `yyyy/MM/dd`, Traditional Chinese weekday labels, and `TWD`.

Modules consume this shared contract rather than choosing a browser-dependent
locale. Future locales can be added beside it without changing module boundaries.
